import { Navbar } from "reactstrap";

const NavigationBar = () => {
  return (
    <Navbar>
      <div>
        <span>Navigation bar</span>
      </div>
    </Navbar>
  );
};

export default NavigationBar;
